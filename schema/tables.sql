create Database CollectionBackend;

use CollectionBackend;

create table users(
    id int auto_increment primary key,
    FirstName varchar(50),
    LastName varchar(50),
    Username varchar(50),
    Age varchar(50),
    Gender varchar(50));

create table docs(
    id int auto_increment primary_key
    
    
)
